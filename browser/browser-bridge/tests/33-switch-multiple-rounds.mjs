// Merged into tests 02+03 (navigate-blank-tab, navigate-default-tab)
// which already do 3 rounds of switch away + switch back.
import { pass } from './helpers.mjs';
pass('switch-multiple-rounds (covered by tests 02+03)');
process.exit(0);
